// 1.  shallow copy and deep copy
//     copy operation in Arrays => shallow copy
//     shares the same reference point
//     changing the copy changes the actual array

// 2. Difference between slice and splice
//    slice doesnt include end number and doesnt changes the array
//    splice includes the end number and changes (removes) the spliced part from original array
//  

// 3. concat and spread operator for array join

// 4. Flat method => converts nested array into a single array
//      takes depth as an argument
//      const arr = [1,2,3,[4,5,[6,7],3],9]
//      arr.flat(depth) depth = 3/Infinity

// 5. Array => Class like Number
//      Array.of() => converts into array
//      Array.from() => also converts into array only which can be (refer the practice in basics-of-array.js)
